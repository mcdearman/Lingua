-- Lingua: compilers written as their specification.
--
-- See docs/DESIGN.md. What is here so far is the runtime a generated parser
-- stands on: the green tree, the red view over it, the event parser, and the
-- builder that makes one from the other.

@pub mod Green
@pub mod Red
@pub mod Parser
@pub mod Build
@pub mod Ungrammar
@pub mod Ast
@pub mod Lang
@pub mod Gen
@pub mod Pass
@pub mod Query
@pub mod Database
@pub mod Diagnostic
@pub mod Cli
@pub mod Lsp
@pub mod Make

@pub use Lingua.Gen (syntax)
@pub use Lingua.Pass (lang, pass)
@pub use Lingua.Database (database)
@pub use Lingua.Cli (cli)
@pub use Lingua.Lsp (lsp)
@pub use Lingua.Make (make)
